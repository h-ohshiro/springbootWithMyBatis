<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
    "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.example.demo.mybatis3.domain.repostiory.CustomerRepostiory">

	<insert id="insert" parameterType="Todo">
		INSERT INTO
		
			

	</insert>

	<select id="select" parameterType="Long" resultType="ToDo">
		SELECT
			*
		FROM
			mybatis
		WHERE
			id

	</select>

	<update id="update">
		<!-- ここにupdateのSQLを書く -->
	</update>

	<delete id="delete">
		<!-- ここにdeleteのSQLを書く -->
	</delete>


</mapper>